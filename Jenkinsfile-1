#!/usr/bin/env groovy
pipeline {
    agent any
    stages {
        stage('Single Stage') {
            steps {
                sh 'echo Single Stage running..!'
                echo "Build number is: #${currentBuild.number}"
                sh 'ls -alt'
                echo env.BRANCH_NAME
            }
        }
    }
}